using UnityEngine;

public class ObjectToMouseCursor : MonoBehaviour
{
    //// Update is called once per frame
    //void Update()
    //{
    //    // Create a ray from the camera through the current mouse position
    //    Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
    //    RaycastHit hit;

    //    // Perform a raycast and check if it hits any collider
    //    if (Physics.Raycast(ray, out hit))
    //    {
    //        // If the raycast hits something, set the position of this GameObject
    //        // to the hit point
    //        transform.position = hit.point;
    //        //Debug.Log($"Object moved to: {hit.point} after clicking on {hit.collider.name}");
    //    }
    //    else
    //    {
    //        Debug.Log("No collider hit by raycast.");
    //    }
    //}
}
